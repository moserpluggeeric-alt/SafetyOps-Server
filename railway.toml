[build]
builder = "nixpacks"

[deploy]
startCommand = "node safetyops-server.js"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
